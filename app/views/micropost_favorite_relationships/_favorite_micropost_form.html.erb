<span class="favorite_micropost_form">
    <% if current_user.favoriting_micropost?(micropost) %>
        <%= render 'micropost_favorite_relationships/unfavorite_micropost', { micropost: micropost } %>
    <% else %>   
        <%= render 'micropost_favorite_relationships/favorite_micropost', { micropost: micropost } %>
    <% end %>
</span>